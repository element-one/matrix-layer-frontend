export enum PaymentStatus {
  SHIPPED = 'shipped',
  COMPLETED = 'completed',
  PAID = 'paid',
  UNPAID = 'unpaid',
  RECEIVED = 'received'
}

export enum PoolType {
  POOL_PHONE = 'pool_phone',
  POOL_A = 'pool_a',
  POOL_B1 = 'pool_b1',
  POOL_B2 = 'pool_b2',
  POOL_C = 'pool_c'
}
